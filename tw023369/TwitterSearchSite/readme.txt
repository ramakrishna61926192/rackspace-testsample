This site allows the use to search Twitter for tweets that contain a phrase.

To set up the site, run the following command in the root folder of the site:
  pip install -r requirements
  
To start the server, run the following command from the root folder of the site:
  python manage.py runserver

Navigate to http://localhost:8000/twittersearch/ and enter your search phrase.
